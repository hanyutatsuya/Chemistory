Cre    再検  0.30以下  20.00以上

推算GFR 性別不明(9)または年齢1未満はエラー
		性別不明の場合は-9,年齢1未満は-99
レンジ下限が0.1になっているが、これは計算エラーを再検にするため
推算GFR自体の再検レンジは無い
関連項目はCre→推算GFR,推算GFR→Creとなっている

対応方法
関連項目マスタの推算GFR→Creを無効にする

期待する動き

①属性正常でCreが正常値の場合、推算GFRを計算して両方報告可能になる
②属性正常でCreが再検値の場合、推算GFRを計算して関連エラーになる（再検低値、高値とも）
③属性正常でCreが再検値の場合、Creの再検データを受け取って照合OKなら推算GFRも初回値が報告可能になる
④属性正常でCreが再検値の場合、Creの再検データを受け取って照合NGなら推算GFRも初回値も照合NG確定画面に表示される

⑤属性エラーでCreが正常値の場合、推算GFRはマイナス値で再検。Creは正常値出あればそのまま報告可
⑥属性エラーでCreが再検値の場合、推算GFRはマイナス値で再検。Creは低値または高値で再検になる
⑦属性エラーでCreが再検値の場合、Creの再検データを受け取って照合OKならCreは初回値にCMT101を付けて報告可能。推算GFRはマイナス値で再検。
⑧属性エラーでCreが再検値の場合、Creの再検データを受け取って照合NGなら推算GFRも初回値も照合NG確定画面に表示される。CreはNG確定したものが報告可能。推算GFRはマイナス値で再検。



保留依頼情報テーブル(HKHRIRAI)あり
HKKHRKBN == 1

結果テーブル
KANRENFLG = '2'
KNSFLG = 'A'
KKAJKCD = '3'
↓

更新
Cre  KNSFLG = 'S'
eGFR KNSFLG = 'H' 
TNYRKFLG = '1' 必要か?

eGFRは属性修正後自動で報告可能になる。

